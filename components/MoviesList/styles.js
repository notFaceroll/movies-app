import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  list: {
    marginBottom: 16,
    paddingVertical: 16,
    borderTopWidth: .5,
    borderColor: '#eee'
  },

  sectionTitle: {
    color: '#eeeeee',
    marginHorizontal: 8,
    fontSize: 24,
    marginBottom: 8,
  }
})