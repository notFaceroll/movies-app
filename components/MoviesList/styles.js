import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  list: {
    marginBottom: 16,
    paddingBottom: 16
  },

  sectionTitle: {
    color: '#eeeeee',
    marginHorizontal: 8,
    fontSize: 24,
    marginBottom: 8,
  }
})